<?xml version="1.0" encoding="UTF-8"?>
<templates>
    <t t-name="bus_demo.GlobalCounter">
        <div class="p-4 border bg-light d-flex align-items-center justify-content-center gap-5 w-100">
            <div id="counterValue" class="h1 fw-bold text-primary">
                <t t-out="state.value"/>
            </div>
            <div class="d-flex flex-column gap-2">
                <button class="btn btn-primary px-3 fw-bold" t-on-click="() => this.changeCounter(1)">+</button>
                <button class="btn btn-primary px-3 fw-bold" t-on-click="() => this.changeCounter(-1)">−</button>
            </div>
        </div>
    </t>
</templates>
